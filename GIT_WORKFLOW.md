# Git Workflow Cheat Sheet

This project uses a simple Git workflow.

## Standard Flow

Any time you make changes, follow this flow:

```bash
git status
git pull
git add .
git commit -m "Describe change"
git push